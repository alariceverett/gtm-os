import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='actions route scaffold'>
      Placeholder for migrating <code>/actions</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
