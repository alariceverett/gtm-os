import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='relationships route scaffold'>
      Placeholder for migrating <code>/relationships</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
