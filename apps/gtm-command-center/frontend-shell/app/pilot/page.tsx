import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='pilot route scaffold'>
      Placeholder for migrating <code>/pilot</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
