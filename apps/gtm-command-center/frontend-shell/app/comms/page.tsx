import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='comms route scaffold'>
      Placeholder for migrating <code>/comms</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
