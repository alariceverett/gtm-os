import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='targeting route scaffold'>
      Placeholder for migrating <code>/targeting</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
