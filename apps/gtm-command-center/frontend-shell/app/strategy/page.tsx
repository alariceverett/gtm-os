import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='strategy route scaffold'>
      Placeholder for migrating <code>/strategy</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
