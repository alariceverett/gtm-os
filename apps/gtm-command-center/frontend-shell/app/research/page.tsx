import { PageCard } from '@/components/page-card';

export default function Page() {
  return (
    <PageCard title='research route scaffold'>
      Placeholder for migrating <code>/research</code> from legacy server-rendered templates into the new app shell.
    </PageCard>
  );
}
